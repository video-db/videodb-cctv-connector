name: Build release Docker image

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: Existing GitHub Release tag to attach the Docker image artifact to
        required: true
        type: string

permissions:
  contents: write

jobs:
  docker-image:
    name: Build and attach Docker image
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "release" ]]; then
            TAG="${{ github.event.release.tag_name }}"
          else
            TAG="${{ inputs.tag_name }}"
          fi

          IMAGE_NAME="videodb-cctv-connector"
          ARTIFACT="${IMAGE_NAME}-${TAG}-docker-image.tar.gz"

          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "image_name=${IMAGE_NAME}" >> "$GITHUB_OUTPUT"
          echo "artifact=${ARTIFACT}" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        shell: bash
        env:
          # If videodb-python is private, configure VIDEODB_PYTHON_GITHUB_TOKEN
          # with read access to github.com/video-db/videodb-python.
          GITHUB_TOKEN_FOR_BUILD: ${{ secrets.VIDEODB_PYTHON_GITHUB_TOKEN || github.token }}
        run: |
          set -euo pipefail
          docker buildx build \
            --load \
            --secret id=github_token,env=GITHUB_TOKEN_FOR_BUILD \
            --tag "${{ steps.meta.outputs.image_name }}:${{ steps.meta.outputs.tag }}" \
            --tag "${{ steps.meta.outputs.image_name }}:latest" \
            .

      - name: Save Docker image archive
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          docker save "${{ steps.meta.outputs.image_name }}:${{ steps.meta.outputs.tag }}" \
            | gzip -9 > "dist/${{ steps.meta.outputs.artifact }}"
          (cd dist && sha256sum "${{ steps.meta.outputs.artifact }}" > "${{ steps.meta.outputs.artifact }}.sha256")

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.image_name }}-${{ steps.meta.outputs.tag }}-docker-image
          path: dist/*
          if-no-files-found: error

      - name: Attach artifact to GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release upload "${{ steps.meta.outputs.tag }}" dist/* --clobber
